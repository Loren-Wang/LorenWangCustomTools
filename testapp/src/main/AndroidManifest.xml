<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.testapp">

    <application
        android:name=".BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ToastActivity"></activity>
        <activity android:name=".FileOptionsActivity" />
        <activity
            android:name=".SmartRefreshLayoutActivity"
            android:label="@string/title_activity_smart_refresh_layout"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GlideImageLoadingActivity" />
        <activity android:name=".BrightnessActivity" />
        <activity android:name=".FileOptionsForScanFileActivity" />
        <activity android:name=".SideBarActivity" />
        <activity android:name=".HorizontalSlipTabLayoutActivity" />
        <activity android:name=".SwitchButtonActivity" />
        <activity android:name=".ImageViewActivity" />
    </application>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission
        android:name="android.permission.WRITE_SETTINGS"
        tools:ignore="ProtectedPermissions" />

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

</manifest>
